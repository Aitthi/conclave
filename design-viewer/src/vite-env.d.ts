/// <reference types="vite/client" />

// Injected by Vite's `define` (see vite.config.ts) — the running viewer
// version, read from package.json at dev-server start and shown in the Topbar.
declare const __ARTA_VERSION__: string;
