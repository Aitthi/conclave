# Repair Skill creation end to end
owner: 2004f459-52ad-445c-9c70-e605a0ffdfe3 (Aoki) · authority: in-loop
Implementer: Dew. Escalation: Aoki. Reading order: diagnosis plan, this plan, task brief.

## Evidence and settled decisions
Diagnosis: docs/superpowers/plans/2026-09-05-skill-assist-diagnosis.md at 584022df; task skill-assist-diagnosis READY note 26a15621. User screenshot names exact symptom. Preserve existing SkillEditor and SkillAssistPanel visual layout at 584022df; historical .arta/prototype/screens/skill-editor-active.html is absent, so current UI plus the original spec is canon. Aoki rules visual exceptions.
Detoro handed SkillAssistPanel ownership to this repair in terminal-filedrop-paste note 5030f95e. Terminal.tsx remains outside boundary.
R1: Fix inaccessible trust/permission prompts by making the real terminal interactive: enable stdin, forward ordered raw xterm onData (including arrow/Enter/control and terminal query replies) to ipc.message.send for current session; clean subscription on dispose. Add concise visible keyboard hint to click terminal when it asks a question. Do NOT programmatically mark any folder trusted or alter ~/.claude.json. The failure is that the app made the CLI's normal interactive step impossible. User retains native CLI choices. Rejected automatic parent-trust write: unnecessary config mutation and reliance on undocumented third-party storage; interactive terminal also solves later permission questions across supported CLIs.
R2: Fit xterm AND resize backend PTY with positive measured cols/rows, pixel dims where available, after layout/resize. Do not send degenerate dimensions. Catch and surface meaningful input failures. Preserve ordered terminal replies. Match working Terminal.tsx behavior without modifying it or undertaking shared-terminal refactor.
R3: Send composer text as bracketed paste, delay 40ms, send standalone raw CR (reference StdinBar.tsx). Clear composer only after both succeed, prevent duplicate sends, display errors. Input >1022 bytes and multiline must arrive whole and submit once.
R4: Restore manual Sync now required by original spec. Successful sync applies name/description/content; failed sync preserves last good values and exposes actionable error. On explicit Stop sync before destructive stopDraftSession deletion; if sync fails preserve editor and draft and show retryable failure instead of silently losing newest file. After that failure reveal an explicit secondary Stop without syncing action, with nearby text: The latest draft could not be read. Stopping without syncing keeps the version shown in the editor and discards unsynced draft changes. Retry Sync remains available. Only that explicit secondary action bypasses sync; a stop failure still keeps the session and allows retry. Clear the fallback when sync succeeds or the active session changes. Stop failure must retain draft and expose retry; unlock only after stop success. Editor close can keep discard/cleanup behavior, but late start result after unmount must be stopped (no hidden leaked session). Capture stable session IDs across awaits so stale async results cannot mutate a newer draft. Keep saved custom skill semantics unchanged.

## Scope
src/components/SkillAssistPanel.tsx, src/components/SkillEditor.tsx, src/fixtures/scenarios/default.ts, src/fixtures/scenarios/empty.ts, scripts/skill-assist-repro.mjs, docs/superpowers/plans/2026-09-05-skill-assist-repair.md. No Rust needed for approved approach. If evidence requires additional paths file a challenge first. Fixture edits confined to skill/session handlers needed for harness. Memory lane must avoid these shared files unless coordinated by Aoki.

## Implementation and validation
Start isolated lane skill-assist-repair. First capture failing behavior in a browser harness exercising actual components and IPC seam (scripts/skill-assist-repro.mjs); establish failing assertion before fix. Exercise terminal keyboard prompt interaction, positive PTY sizing at start and resize, paste+standalone CR ordering and long/multiline input, sync success/failure, final sync then stop then save, stop failure retry, close during unresolved start cleanup. Use controllable fixture event hooks or injected module seam; never swallow fixture errors. Fixed literal timestamps only. Preserve skill library data across save/reopen in harness.
Run pnpm build and harness gate. Run pnpm uishot library and pnpm uishot library --scenario empty, OPEN and LOOK at both PNGs, and attach paths to READY; record each with conclave task gate. Additionally screenshot and inspect actual opened editor idle/active/synced states (plain library capture does not prove editor rendering). Before any shot inspect lsof :1420 and server cwd; coordinate with Aoki before killing a peer server. Any live CLI test uses isolated temporary configuration/cwd; do not answer trust prompts on behalf of human in their real config, modify production skills, or run unrelated work. Use harness proving native trust prompt input route and real PTY resize; model generation smoke can be requested from Aoki if necessary, not from human directly.
Commit with stage commit; READY note includes commit, test results, shot absolute paths and any real-CLI coverage limits. Do not merge own lane. Aoki integrates after review/rerun.

## R4 amendment — Dew challenge 9f95a320
Accepted 2026-09-05. read_draft returns None on a missing/unparseable SKILL.md, and skill_draft::sync returns Invalid, so a strict successful-sync prerequisite has no recovery while keeping the editor open. The original plan overlooked permanent failure (found by Dew). Explicit Stop without syncing provides informed escape without silently discarding draft content. Regression must simulate permanent missing/unparseable draft, assert first Stop keeps session, explicit fallback stops and unlocks while preserving the displayed version, and failed fallback stop remains retryable.
