# Restore usable Memory Knowledge Graph
owner: 2004f459-52ad-445c-9c70-e605a0ffdfe3 (Aoki) · authority: in-loop
Implementer: Dabin. Escalation: Aoki. Reading order: diagnosis plan 2026-09-05-memory-graph-repair.md, this plan, task brief memory-graph-repair for evidence.

## Diagnosis and decisions
User screenshot /Users/detoro/Desktop/Screenshot 2569-09-05 at 11.06.33.png shows 277 memories/584 links collapsed horizontally. Task memory-graph-repair notes e461008e/da6bdc99 reproduce near-blank viewport at 278 nodes; df3fb6cb isolates collision branch and restores all 279 nodes by changing only non-unit collision vector. Note 8a25ca21 finds historical authors mapped to Shared and duplicate React keys. Challenge 2cf630ab accepted by Aoki after inspecting code and probe pixels. Current MemoryGraph layout is canon; historical .arta/proto/screens/memory-graph.tsx at c134d01 is unavailable in checkout. No redesign.
R1: Replace d2<1 fallback dx=i-j with deterministic unit direction so force magnitude no longer scales with array-index distance; both x and y get bounded components. Preserve pair symmetry, determinism, existing simulation/fit limits. Do not remove fit floor to conceal unbounded simulation. All positions finite and all 277 stress nodes visible after initial warmup/fit.
R2: Carry stable source identity key through group records and key rows by that identity. Shared only for null sourceId. Non-null unresolved author label Former agent plus stable 8-character source ID prefix; retain neutral violet since name/colour is unavailable. Resolve agent AND distilled source identities from current roster when present. Do not invent lost names or merge distinct identities. Prefix collisions must still retain distinct React keys and full-id tooltip if needed. Add distilled to MemoryGraphNode.sourceKind in src/ipc/types.ts and update its sourceId comment to match backend memory.approve. This is a type correction, not backend/API behavior change.
R3: Keep settings panel inside available viewport, with bounded scrolling and accessible lower controls at 900px and constrained heights. Preserve floating header and existing panel style. Graph initial fit and manual fit should use usable space without hiding nodes behind panel; if existing padding cannot satisfy stress capture, record minimal adjustment evidence rather than changing topology/fit floors.
R4: Canon exception approved for dense graphs (>80 nodes): suppress global overview labels regardless of ordinary 0.95 zoom threshold, but retain hovered/selected focus labels, its immediate-neighbour labels and search-match labels. Sparse graphs retain current zoom label behavior. This avoids the newly exposed 279-label cloud; no new density controls or dependency. Verify selection, hover, clear selection, search/clear, zoom/pan and drag still work.

## Boundary and coordination
src/components/MemoryGraph.tsx; src/ipc/types.ts (MemoryGraphNode only); scripts/memory-graph-stress.mjs; docs/superpowers/plans/2026-09-05-memory-graph-fix.md. New isolated lane memory-graph-fix, no commits in diagnosis lane. Do not modify src/fixtures/scenarios/default.ts or empty.ts (Dew skill-assist-repair owns both). Harness injects dedicated deterministic stress payload at runtime. Existing :1421 diagnosis Vite may be reused by Dabin; request :1420 window from Aoki before required uishot capture; never silently use foreign checkout server.

## Gates and expected evidence
Create repeatable real-component Puppeteer regression in scripts/memory-graph-stress.mjs with >=277 nodes/584 edges, fixed IDs/timestamps, mixed current/former/manual/distilled authors. Demonstrate old code fails before fix (retain pre-fix command/result); fixed code has finite bounded positions, visible nodes after initial fit and resize/refit, correct counts, one true Shared group, unique former labels/keys and no console errors. Exercise focus/neighbours/search, label density, pan/zoom, filters and panel scrolling. Inject data without product test globals where feasible. Do not commit live user memory text or raw production DB exports in test fixtures; synthetic deterministic topology is sufficient after reproducing real payload.
Run pnpm build, stress harness, pnpm uishot memory and pnpm uishot memory --scenario empty. Record each with conclave task gate. OPEN/LOOK at all screenshots using image reader; attach absolute paths in READY. Also capture stress default, stress focus/search and constrained-height panel, preferably dark theme matching user's screenshot; inspect light for contrast when affordable. Verify lsof :1420 plus server cwd before trusting shots. Keep files unchanged outside boundary and preserve memory data. READY includes commit, failure-before/pass-after evidence, gate IDs and shot paths. Aoki reviews/reruns/integrates, implementer does not merge.
