# fmt sweep: bring pristine main back to `cargo fmt --check` green under the pinned 1.96.0 toolchain

owner: 30fa04f4-e047-4241-a9ed-f452529952be (Detoro, Lead) · authority: in-loop
priority: LOW-MEDIUM — every lane's fmt gate is currently red on pristine main (blackboard `warning:fmt-drift-main`).

## Facts

- As of main 1f8e056 (and still at 98fbbe9), `cargo fmt --check` in `src-tauri` fails on a clean checkout with
  the pinned toolchain (rust-toolchain.toml 1.96.0, rustfmt 1.9.0-stable): 17 files, none owned by a live lane —
  `crates/codeintel/src/{edit.rs,edit/rename_cmd.rs,edit/rewrite.rs,index.rs,map.rs}`,
  `crates/codeintel/tests/{cache_test,edit_test,index_ext_test,lang_new_test,map_test}.rs`,
  `src/engine/agentctx.rs`, `src/engine/commands/{browser,instance}.rs`, `src/engine/repo/workspace_agent.rs`,
  `src/engine/runtime/{browser,task_timer}.rs`, `src/lib.rs`.
- The 2026-07-09 rule (memory: "codeup Rust gates") says fmt/clippy/test are required lane gates and a red fmt on
  pristine main means "suspect a toolchain bump vs the pin" — verify which side moved before formatting.

## Tasks

1. Confirm whether the drift is code committed unformatted or rustfmt output changing under 1.96.0 (`rustup run 1.96.0
   cargo fmt --check` vs the toolchain the drifted commits were made with — `git log -1 --format=%H -- <file>`).
2. One commit, formatting only, no other changes: `cargo fmt` in `src-tauri`, verify `git diff --stat` touches only
   those 17 files, `cargo test` green.
3. Delete blackboard key `warning:fmt-drift-main`; note the outcome on this task.

## Gates

`cargo fmt --check` (tree-wide, green), `cargo clippy --all-targets -- -D warnings`, `cargo test`.

## Boundary

The 17 files above only. Coordinate with any lane that claims one of them first (check `conclave task list`).
