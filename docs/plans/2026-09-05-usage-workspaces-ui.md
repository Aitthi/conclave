# AI usage Overview and reversible workspace management UI
owner: 2004f459-52ad-445c-9c70-e605a0ffdfe3 · authority: in-loop
Implementer: Dabin (21a29434-cfd8-4b6e-a519-4f2a2a29d093). Aoki rules product/API conflicts and integrates. Hardwell is the design escalation target through Aoki. Execution slug: usage-workspaces-ui.

## Reading order and start gate
Read PRODUCT.md → docs/plans/2026-09-05-usage-overview-contract.md → docs/plans/2026-09-05-workspace-archive-engine.md → docs/design/2026-09-05-workspace-overview-archive.md → docs/plans/2026-09-05-usage-engine.md (Frozen IPC and Preflight correction) → this plan.

Design canon: design/screens/workspace-overview.tsx and workspace-archive.tsx with their PNG atlases at f4c94f79ddc6; final wording and interaction handoff docs/design/2026-09-05-workspace-overview-archive.md at dbc6d6b. The later source/basis/unscoped/zero rulings in the canonical contract and engine plan override specimen text, never its accepted geometry. Do not redesign these views.

Read-only preflight is authorised immediately. Do not lane-start or edit src until Aoki posts the exact phrase USAGE FOUNDATION MERGED on this task, naming the merged main SHA. Then use conclave lane start from that main. Dew owns src/ipc/types.ts and commands.ts throughout usage-engine-v2; consume the merged types, never duplicate a speculative local wire or edit those files. Aoki will merge the tested foundation while collectors continue in Dew's lane. This UI may be verified against honest no-coverage engine responses; the overall feature is not complete until collectors are integrated too.

## File boundary
- src/components/AppShell.tsx
- src/components/Rail.tsx
- src/components/EditWorkspace.tsx
- src/components/UsageOverview.tsx
- src/components/UsageHeatmap.tsx
- src/components/WorkspaceManager.tsx
- src/components/usage-overview.css
- src/components/workspace-manager.css
- src/fixtures/backend.ts
- src/fixtures/scenarios/default.ts
- src/fixtures/scenarios/empty.ts
- src/fixtures/scenarios/data.ts
- src/fixtures/scenarios/usage.ts
- src/fixtures/scenarios/workspaces.ts
- scripts/uishot.mjs
- docs/plans/2026-09-05-usage-workspaces-ui.md (lead amendments only)

Keep helpers local to these new components or the fixture usage module. No global token changes, backend changes, dependency additions or unrelated component refactors. Challenge before adding another path. One UI lane owns these coupled navigation, management and fixture seams; splitting them would create shared-file contention.

## Navigation and shell integration
The Conclave brand mark opens app-global Overview. Use the existing public/brand/logo-mark.svg asset with an accessible Overview name and visible selected state. Overview is the normal initial destination, with no implied selected-workspace roster. Add the separate Workspaces destination as shown in the canon, retaining workspace Rail entries, Link folder, Agent Library and Skill Library. Navigation never starts or stops an agent or workspace.

Represent the two global destinations explicitly and mutually exclusively with workspace centers. Reuse existing shell architecture with a small global destination state; a routing rewrite is outside scope. Global destinations occupy the full area beside the 56px Rail, hide Roster/context panes, and do not leak a selected-workspace toolbar or terminal. Retain a last workspace selection internally if useful, but do not present it as active while in a global view. Reconcile every existing visibility predicate, including full-window browser/design/artifacts, so users can leave each surface and return without trapping navigation or starting runtimes. Existing fixture #view=home still targets the workspace home; missing fixture view defaults to overview.

Include the titlebar's 266px Roster background and menu/keyboard entry points in the visibility audit. Clear Browser when entering a global destination so its independent full-window predicate cannot conceal Rail. On active membership loss, also clear stale agent/generation and center state. Route LinkFolder success through the common selection transition so linking from a global view actually opens the new workspace.

Opening an active workspace explicitly calls the existing workspace.use flow and shows its home only after success. A refused/missing/archived workspace keeps the user in management with a useful inline error; do not reuse the current swallowed rejection. A restored workspace remains stopped until a separate explicit Start action. Selecting a workspace does not call Start.

Load active and archived workspace lists through workspace.list and workspace.listArchived. Initial errors must render an error/retry state, not a false empty list. Refetch on workspace:changed even when runState is unchanged; archivedAt is independent. Retain safe start/stop event reconciliation while an owned operation is pending. Use request identity or equivalent stale-response protection so late list or usage results cannot restore archived Rail entries or overwrite newer filter results. If an externally archived/deleted workspace is currently open, clear its visible workspace selection and show Workspaces. Rename/color changes refresh both management and Rail. Do not replace a valid newer command result with an older list response.

## Usage Overview
Call typed ipc.usage.overview with days 90 initially (30 alternative), browser IANA timeZone, and optional workspaceId/workspaceAgentId/modelKey. Use one in-flight request for the current filter generation; ignore obsolete completions. Refresh when entering the view, on filter/range changes and explicit Retry/Refresh; while visible, refresh every 10 seconds without overlapping requests. Stop the timer on exit. Retain the last successful same-filter result on refresh error with a visible stale/error message; do not show old-filter data under new labels.

Render returned daily calendar dates and range as authoritative. Do not reconstruct daily UTC intervals with 24-hour arithmetic or use the specimen's frozen Bangkok bucketing. Format date-only keys without browser UTC shifts. Align all summary numbers, heatmap and three breakdowns to one response. Model/workspace/agent selectors remain discoverable using returned options, including archived history, No workspace (__unscoped__) and Unassigned activity (__unassigned__). Synthetic IDs only enter usage filters, never workspace lifecycle callbacks. When workspace changes, clear a selected agent that belongs to a different workspace; the unassigned filter may stay and is scoped by workspace.

Always show Reported or Selected basis beside known model names in selectors and model rows; neither known becomes Unknown model. Keys are opaque, and selected/reported identities with the same name stay separate. Do not invent model attribution on aggregate agent/workspace rows that do not carry it. Coverage shown for model rows is the enclosing filtered scope, not a per-model guarantee.

Use the exact headline Recorded activity and helper Model responses and draft runs. Show responseCount and invocationCount in summary/breakdown secondary text and daily details/table. Measured tokens comes directly from measuredTokens, with unknownUsageCount visibly paired; input/output subtotals stay separate. Never add cache/reasoning again, sum context, infer pricing or label a subtotal as account consumption. A numeric zero is real only when returned as zero. A null measuredTokens stays an em dash even with complete activity coverage and existing unknown-token events.

Coverage states must remain visually distinct: complete empty activity is 0; partial empty activity is an em dash with partial status; none is Unavailable. Observed activity on partial coverage retains its exact count and marker. Display collection start/last verification, pending import and unsupported source status in concise product language. No-events, no-coverage, loading and request failure are different states. Avoid showing internal parser/schema identifiers in product copy.

Heatmap follows canon: seven weekday rows, chronological week columns, month/date labels, 16px cells and 6px gaps, scoped indigo 0/1/2/3/4+ ramp. Outside-range slots are blank/noninteractive. Unknown is distinct from zero. Today is in progress. Each real day has accessible exact date/activity/response/draft/coverage details and hover/focus content. Roving tabindex and arrow keys move between real cells, stopping propagation to app shortcuts. Provide the daily table toggle with all exact values and no dependence on color.

Current context remains a separate latest-per-agent section, independent of range and subject to identity filters. Show used/capacity, model basis from the matching model option, actual source, observation time/age, workspace and Archived label where relevant. Null tokens/capacity/source/time remain unknown, with no percentage if denominator is unknown/nonpositive. Do not treat a missing observation timestamp as fresh. Long names and paths truncate with accessible full text.

## Workspace management and EditWorkspace
Use active/Archived tabs, search, list loading/error/retry and distinct no-workspaces/all-archived/no-archive/no-search-match states from the canon. Active includes stopped workspaces; archived is separate from stopped. Do not hide retained history from Overview. Open, settings and Link folder connect to existing shell actions, without inventing a native picker mock in production.

Extend production EditWorkspace with Archive/Restore while preserving existing rename, predefined/custom color picker and separate permanent Delete confirmation. Archived workspaces cannot be renamed/recolored until restored; make that state clear. A started workspace (including one with zero runtimes) offers explicit Stop using the existing warning; stopping must not automatically archive. A stopped workspace offers Archive, subject to fresh backend validation. Pending mutations disable repeated submission and dismissal. Busy/race refusals remain inline with the workspace row intact. Do not duplicate backend lifecycle decisions from stale UI status.

Replace the active-only settings boolean with an explicit target workspace ID resolvable from either collection; key EditWorkspace by that ID because form state initializes once. Every mutation targets that row, never the selected workspace by inference. Delete clears workspace selection only if the deleted ID matches it. Return archive/restore rows through explicit callbacks for reconciliation. Management Stop takes an explicit workspace ID; the existing selected-workspace Stop handler alone is insufficient. Reuse the existing Roster Stop warning wording without editing Roster. Use a native dialog for focus containment and a single mutation lock across save/delete/stop/archive/restore.

Archive success removes the workspace from active list/Rail and offers Undo through Restore. Restore remains in the Archived destination, removes the restored row, preserves a stopped state and offers explicit Open in success feedback. Undo is also Restore, never Start or automatic Open. If archiving the selected workspace, remove its visible selection. Permanent Delete remains a distinct confirmed action in settings for active or archived workspaces, using existing cleanup behavior. Keep focus sensible after row removal, dialog dismissal and success feedback; use an accessible live region for mutation results.

Use existing surfaces, font and controls with scoped styles for new states. Apply --archive-danger-text light #b42318 / dark #ff8a80 to archive/error text with the accepted 10% alert tint; no global danger token changes. Minimum supported view is 880x560. Dialog body scrolls within the viewport while controls remain reachable; preserve focus containment and Escape when idle, block dismissal only during mutation.

## Deterministic fixtures and verification
Extend the typed fixture backend for usage.overview, workspace.listArchived, archive/restore and used workspace mutations. Timestamps are fixed literals only, including fixture generatedAt. Route all added scenarios through the existing DEV fixture seam; production imports remain tree-shaken. Never swallow missing fixture handlers. Reuse the production components and shell, not a specimen clone. Preserve existing home/library/builder fixture behavior.

Provide view routes overview, workspaces, archived and workspace-settings, plus explicit deterministic fixture variants for Overview loading/error/none/partial/verified-empty and workspace all-archived/archive-error/restore-error/started/busy. Reuse --scenario default|empty as the mandatory base; additional named scenarios may be added in the existing loader/script. Preserve the legacy empty fixture's stopped workspace; true-zero/all-archived states belong in the new workspaces fixture module. Ensure readiness means the requested state rendered, including intentional loading/error specimens. Extend the current boot/Builder readiness checks to wait for new-view data and modal preparation. Lifecycle handlers share mutable fixture collections and emit workspace:changed through the existing EventTarget; stateless success echoes are insufficient. No fixture event infrastructure rewrite is needed.

Default usage data must exercise known plus missing tokens, Reported/Selected models with the same name but distinct keys, Unknown model, archived workspace history, No workspace and Unassigned activity, complete-zero/partial-empty/none dates and a partial current day. Filtering the fixture must actually change all aggregates/daily/breakdowns consistently; do not just change a label on an unfiltered object. Include the counterexample complete coverage + observed activity with all usage unknown => measuredTokens null. Use fixed dates around a DST transition or a focused deterministic check to prove chart labels do not shift in a different browser timezone; backend owns bucket arithmetic tests.

Run pnpm build and scoped git diff --check. Verify interactions through conclave browser only: global navigation and return from full-window browser/artifacts/design; model/agent/workspace filters and 30/90 days; exact daily table and keyboard grid behavior; archive refusal after state changes; separate Stop then Archive; Archive/Undo; Restore then explicit Open; external workspace:changed reconciliation; rename/color; separate Delete confirmation. Fixture interaction runs must never invoke real archive/delete/start commands. Record actual outcomes, not just ok:true from click. If an automation verb fails, record verb/selector/result and route to Aoki; no standalone browser fallback. Close the owned tab. Human-required pnpm uishot is the approved screenshot exception.

UI pixel gate before READY: check lsof -nP -iTCP:1420 -sTCP:LISTEN and verify listener cwd is this lane. Stop only a verified foreign dev server before capture. Run conclave task gate WS usage-workspaces-ui -- pnpm uishot VIEW for each affected view, repeat --scenario empty when empty state is affected, OPEN and LOOK at every written PNG, and put paths in the READY note. Required new views: overview, workspaces, archived, workspace-settings (default and empty where applicable), plus all added error/pending/started variants. Shared Rail/AppShell changes affect home, laneboard, memory, artifacts, blackboard, chat, library, builder, builder-edit, browser and settings: run/open these regression shots too, including existing empty variants when their selection/empty behavior changes. Inspect both light/dark and 880x560 for the new views; use script viewport/theme options or scoped fixture routes, not alternative browser automation. Builder captures requiring the lower form use --viewport 1440x1900, not --full.

## Risks, handoff and completion
Fragile seams: independent archive/runState events; shell visibility booleans and full-window browser; stale asynchronous list/filter requests; model basis and unscoped IDs; unknown versus zero; fixture clock versus production timezone; synchronous Tauri getters in plain Chrome. Wrap any unavoidable render-path native getter call itself in try/catch, not only its returned promise. Existing fixture PTY panes being blank is accepted. Keep operation failures visible and preserve the user's filters/tab.

Post UI READY with commit SHA, actual pixel paths and gate IDs, interaction findings and any limitation. Move the task to review, never self-merge. Aoki reviews actual production pixels against canon, reruns appropriate integration gates and owns merge/lane cleanup. Independent design and behavior review may request corrections within this scope. No app rebuild/relaunch, deployment or publishing is included. The human's manual checklist remains final acceptance.


## U1 failure-path completion — ruling c9d88cdd
Concurrent external Archive may win its write lock while a pending Save waits for a read lock. The archive event removes the active row; Save then legitimately rejects the archived target. If the event-triggered archived-list query also fails, preserve the settings target snapshot and visible Save error until explicit dismissal or authoritative target reconciliation. A pending flag alone must not control snapshot lifetime. Explicit dismissal clears target identity and snapshot, so a later successful list retry cannot reopen settings. The same archive operation emitting then rejecting was not a supported trigger and is expressly withdrawn; the external Archive versus Save ordering was reproduced through the original fixture update.

Standalone correct-behavior gate: `/tmp/aoki-ui-external-save-acceptance.py`, artifact `54312950-0269-43b1-8d2a-eb89a17b8278`, SHA256 `ac474936af758ab50fa0acb9767ff48305349eefb6c2a60720b4e62ad1d191de`. Run at final HEAD with `conclave task gate 11ecf99b-53f4-4c24-b538-b19e5933a9e3 usage-workspaces-ui -- python3 /tmp/aoki-ui-external-save-acceptance.py --base-url http://localhost:1420` on the verified lane server. Preserve unchanged v2 all-six regression gate and affected-view uishot/open/pixel plus build gates. Root independently reproduced the defect at f6912055/e2c6b46a; U2-U6 and final design acceptance remain settled.
