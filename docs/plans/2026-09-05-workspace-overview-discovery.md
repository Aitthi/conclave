# App overview and workspace archive discovery
owner: 2004f459-52ad-445c-9c70-e605a0ffdfe3 · authority: in-loop
Researcher: Marty (0ed6b21b-8322-46c6-868c-8df84218bd30); Aoki owns final product/architecture decisions.
User authorized two features: app-wide overview spanning all workspaces; reversible workspace archive since deletion is currently the only removal.

Read PRODUCT.md, then map actual workspace model/migrations/repo, IPC commands/events, lifecycle launch/stop/resume/delete, app navigation/selection/persistence, fixture seams, and scoped aggregate queries available for all-workspace overview. Use code intelligence. No source changes.
Output docs/research/2026-09-05-workspace-overview-archive.md with bounded evidence: exact files/symbols/fields, ALL external/CLI launch routes requiring archive protection, current list/get semantics, existing workspace status meanings, active process definition, safest transaction/lifecycle boundary, compatibility constraints, migration/test commands, and proposed backend/UI file partitions. Include what current counts mean (working, active session, task states) and avoid N+1 overview aggregation. Describe accessible test/fixture paths for archive/restore and overview zero/mixed/all-archived states.
Product starting point to evaluate: separate archivedAt from runtime status; archive retains all project/workspace records and files, hides from normal navigation, restore does not start agents; prohibit archive while workspace has running agents rather than silently terminate work. Overview has Active and Archived workspaces, each with agent/task activity and open/restore actions. Identify concrete contradictions and propose smallest safe adjustment.
Task boundary docs/research/2026-09-05-workspace-overview-archive.md. Research only, can author on main with scoped stage commit; no implementation. READY exact phrase DISCOVERY READY, report compact path/commit and blockers. No polling other agents. Aoki rules inside loop.

## SUPERSEDING HUMAN RULING — 2026-09-05
Detoro relayed and durably recorded bb ruling:overview-scope: human says "Overivew ให้เป็น พวกการใช้งาน ai model". Overview is AI MODEL USAGE across workspaces, not a generic workspace dashboard. This section supersedes the overview parts above. Archive behavior/scope remains unchanged.
Re-aim research at existing per-provider usage/context telemetry, CLI transcript parsing vs engine chat usage, model identity and model changes, per-agent/workspace attribution, persisted session/time-series data, token field semantics and sources, event freshness, total vs current-context distinction, and restart/resume/double-counting. Need explicit matrix of available/missing data by provider/harness and concrete minimal instrumentation needed for an honest over-time view. Do not equate context occupancy with billed/cumulative tokens. Unknown coverage must remain unknown, never zero. No fabricated pricing/cost or token estimates presented as measurements.
Propose aggregate query/data contract for per-model / per-agent / per-workspace tokens + context over time and supported time ranges, with bounded efficient queries, retention and temporal bucketing. Investigate coverage of existing/history data; show prospective collection start when history unavailable. Continue archive seam audit independently. Output same report path, now organised Usage Overview then Archive; all stale workspace task-count metrics are out of Overview scope.

### Additional human example: token figures + activity heatmap
Updated bb ruling:overview-scope, reference docs/research/2026-09-05-overview-activity-reference.png at 909ed66. Usage figures and GitHub-contribution-style activity heatmap are explicit deliverables. Research which DURABLE semantic activity records can be counted per day (and optionally hour), with source/coverage and timezone semantics. Prefer persisted agent turns/messages or stable usage events over transient terminal byte/output chunk counts. Explain definition and dedup identity; users must know what a cell counts. Compare daily calendar versus day/hour layout against available data. Empty historical coverage differs from measured zero activity. Need API bucket timestamps/range/timezone and honest retained history limits. Do not implement yet.
